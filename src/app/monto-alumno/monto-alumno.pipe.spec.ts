import { MotoAlumnoPipe } from './moto-alumno.pipe';

describe('MotoAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new MotoAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
